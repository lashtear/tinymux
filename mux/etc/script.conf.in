# script.conf - constants for mux shell scripts
#
# Change these variables as appropriate
#
OWNER=mux_admin@your_site.your_domain
DBDATE=$(date +%Y%m%d-%H%M%S)
#
### Change things below here ONLY if running multiple games ###
### in the same install                                     ###
#
# NOTE: DATA, and TEXT here -must- agree with the settings in
# your netmux.conf file.  The ./Startmux, ./Backup, and _backupflat.sh
# scripts depend on this assumption.
#
# BIN=bin
# TEXT=etc/text
# DATA=var
# LOGDIR=var/log
# RUN=var/run
#
PATH=@bindir@:@libexecdir@:$PATH
BIN=@bindir@
TEXT=@sysconfdir@/text
DATA=@localstatedir@
LOGDIR=@localstatedir@/log
RUN=@localstatedir@/run
#
NEW_DB=$DATA/netmux.db.new
INPUT_DB=$DATA/netmux.db
GDBM_DB=$DATA/netmux
CRASH_DB=$DATA/netmux.db.CRASH
SAVE_DB=$DATA/netmux.db.old
PIDFILE=$RUN/netmux.pid
#
